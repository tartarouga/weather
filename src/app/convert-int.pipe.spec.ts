import { ConvertIntPipe } from './convert-int.pipe';

describe('ConvertIntPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertIntPipe();
    expect(pipe).toBeTruthy();
  });
});
